//HalfAdder Tarea

CHIP HalfAdder {
    IN a, b;    // 1-bit inputs
    OUT sum,    // Right bit of a + b 
        carry;  // Left bit of a + b

    PARTS:
    // Sum = a XOR b
    Xor(a=a, b=b, out=sum);
    
    // Carry = a AND b
    And(a=a, b=b, out=carry);
}
